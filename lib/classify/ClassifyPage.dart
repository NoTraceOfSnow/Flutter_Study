import 'package:flutter/material.dart';

class ClassifyPage extends StatefulWidget {
  @override
  createState() => new ClassifyPageState();
}

class ClassifyPageState extends State<ClassifyPage> {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Text("2222"),
    );
  }
}